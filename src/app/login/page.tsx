// app/login/page.tsx
import LoginForm from "@/components/auth/LoginForm"; // Adjust path if needed

export default function LoginPage() {
  return (
    // You can add any page-specific layout or wrappers here if needed
    // For example, a component that sets page title metadata
    <LoginForm />
  );
}
